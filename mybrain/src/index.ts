import { ViewController } from './entry.views';

window.onload = () => {
    ViewController.start();
}

/*
var c = document.getElementById("myCanvas");
var ctx = c.getContext("2d");

ctx.fillStyle = "#FF0000";

ctx.beginPath();
ctx.arc(10, 10, 50, 0, 0.5*Math.PI);

ctx.arc(90, 90, 50, Math.PI, 1.5*Math.PI);
ctx.fill();
*/