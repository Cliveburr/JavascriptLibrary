import { ISAttribute } from "../../dom/models";
import { SNode } from "../../dom/snode";
import { AttrResolver } from "./attr.provider";

export class ForAttr implements AttrResolver {
    
    public build(snode: SNode, attr: ISAttribute): ISAttribute | undefined {

        // pegar os childs

        // criar um snode para ser o for

        // dar replace desse for com o snode original

        return undefined;
    }

}

export class ForCode {
    
}